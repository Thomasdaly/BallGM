# Project instructions for Codex

This file used to duplicate the full instruction set. It now just points at the canonical copy so the two can't quietly drift apart — read **`CLAUDE.md`** in the repository root before doing anything else. Every rule there (architecture boundaries, coding/testing standards, safety and legal boundaries, change control, definition of done) applies exactly the same way here, regardless of which agent is reading it.

The only thing worth restating: docs under `docs/` are load-bearing, not historical. Read the relevant one before touching a system you haven't worked in yet, and update it when behavior or architecture actually changes.
